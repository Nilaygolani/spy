services:
  - type: worker
    name: spyware-client
    env: python
    python_version: 3.10          # a real, supported version
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    region: oregon
